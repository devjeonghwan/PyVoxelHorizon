from enum import Enum, auto


class PadButtonType(Enum):
    LEFT_BUMPER = auto,
    RIGHT_BUMPER = auto,
    UP = auto,
    DOWN = auto,
    LEFT = auto,
    RIGHT = auto,
    A = auto,
    B = auto,
    X = auto,
    Y = auto
