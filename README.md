# PyVoxelHorizon

1. Build `windows/PyVoxelHorizonBridge` for Windows 64bit
2. Copy `PyVoxelHorizonBridge.dll` from build destination directory to `pyvoxelhorizon/` directory
3. Run `attach_to_vh.bat`
